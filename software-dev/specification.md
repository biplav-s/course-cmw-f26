# Specification template
_ // A highly simplified way for developers to tell what their system-to-be-built will do against the requirement_

### 1. Description: 
_ // Say what will be done in plain language corresponding to which requirement. Now list the specifications as a list against which the developed system can be tested._

// Example 1: We will build a system to sort numbers which are integers and decimals.
// Example 2: We will build a system to sort numbers which are numeric, decimals and real numbers.

### 2. Requirements list: 
_ // Format: Identifier: Say what the system will do in detail so that it will be tested._

// Example: S1 (for R1): When N numbers are given as input, N numbers in non-descending order will in the output

// Example: S2: When no number is given as input, the output will be empty.

