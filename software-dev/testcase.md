# Testcase template
_ // A highly simplified way for testers (developers side) to tell what their **built system** will do against the specification.

### 1. Description: 
_ // Say what should be done in plain language corresponding to which requirement. Now list the specifications as a list against which the developed system can be tested._

// Example 1: We will test the system to sort numbers which are numeric, decimals
// Example 2: We will test the  system to sort numbers which are numeric, decimals and real numbers.

### 2. Testing list: 
_ // Format: Identifier: Say what the system will do in detail so that it will be tested._

// Example: T1 (for S1): When N numbers (N,N-1,...,1) are given as input, N numbers in non-descending order will in the output (1,2,..N-1, N).

// Example: T2: When no number is given as input, the output will be empty.

// Example: T3: When inputs are all 0s for N length, the output will be 0s for N length.

